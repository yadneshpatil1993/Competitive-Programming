#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int main()
{
	int n,i=0;
	cin>>n;
	string DNA,enz[n];
	cin>>DNA;
	for(i=0;i<n;i++)cin>>enz[i];
	int p;
	for(i=0;i<n;i++)
	{
		p=enz[i].length();
		int count=1;
		while(count>0)
		{
			
		}
	}	
	return 0;
}